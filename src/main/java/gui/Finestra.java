package gui;

import javax.swing.*;

public class Finestra extends JFrame {
    public Finestra(){
        setTitle("ScaleESerpenti");
        setSize(1200,650);
        setLocation(0,0);
        setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);
    }
}
